# Sacred Rules of Japan — Traveler's Etiquette & Manners Guide

An elite, ultra-modern, interactive directory of Japanese etiquette, rules, and local laws designed for respectful travelers.

## 🌟 Key Features

* **Zero-Lag Architecture**: Fully optimized to run at a buttery-smooth 60fps. Zero heavy particle systems, no layout thrashing, and zero dependencies.
* **Premium FLIP Transitions**: Uses the hardware-accelerated **FLIP (First, Last, Invert, Play)** animation technique. Cards fly seamlessly from their exact grid position to the center of the viewport and flip to reveal details.
* **Snappy Search & Filtering**: Real-time fuzzy search and a kinetic, drag-to-scroll category filter bar to browse by Dining, Transit, Social, and Temple etiquette.
* **Severity Tag Controls**: Quick-toggle between `🔴 Critical`, `🟡 Important`, and `🟢 Normal` levels, or sort by `⭐ Most Useful` (default priorities).
* **Asymmetrical Bento Grid**: A gorgeous, responsive grid layout utilizing modern Plus Jakarta Sans and Noto Serif JP typography.

## 🛠️ Tech Stack

* **Structure**: Semantic HTML5
* **Styling**: Modern Vanilla CSS (with noise texture overlay and custom Bezier easing)
* **Logic**: Vanilla ES6 JavaScript (zero dependencies)

## 🚀 How to Run Locally

Since this is a lightweight, frontend-only project, no compilation or local installation is required.

1. Clone the repository:
   ```bash
   git clone https://github.com/amandeepintl/GuideToJapan.git
   ```
2. Open `index.html` directly in your web browser of choice:
   * **Windows**: Double-click `index.html` or run `Start-Process index.html` in PowerShell.
   * **macOS/Linux**: Run `open index.html` in your terminal.

## 📁 Repository Structure

```
GuideToJapan/
├── assets/          # Static media and design resources
├── details-docs/    # Raw source documentation and reference texts
├── js/
│   └── rules-data.js # Structured JSON-like travel rules database
├── index.html       # Single-page application template and styling
└── README.md
```
